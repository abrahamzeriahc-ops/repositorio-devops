version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.9
  build:
    commands:
      - echo "Construyendo la imagen de Docker..."
      - docker build -t mi-app-devops ./app
  post_build:
    commands:
      - echo "Construcción en LocalStack finalizada."